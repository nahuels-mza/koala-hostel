import { makeStyles } from "tss-react/mui";

const useStylesAppGenerals = makeStyles()((theme) => ({
  container: {
    width: "100%",
    alignContent: "center"
  },
}));

export default useStylesAppGenerals;
